@page "/inks/edit/{id:guid}"
@model LabelsMis.Web.Pages.Inks.EditModel
@{
    ViewData["Title"] = "Edit ink";

    ViewData["BackNav"] = new BackNavModel();
}

<h1 class="h3 mb-3">Edit ink</h1>
<form method="post">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <partial name="_InkForm" model="Model.Input" />
    @if (Model.CanEdit)
    {
        <button type="submit" class="btn btn-primary">Save</button>
        @if (Model.IsActive)
        {
            <button type="submit" asp-page-handler="Deactivate" class="btn btn-outline-danger"
                    onclick="return confirm('Deactivate this ink?');">Deactivate</button>
        }
    }
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
