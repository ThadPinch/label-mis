@page "/finishing-operations/edit/{id:guid}"
@model LabelsMis.Web.Pages.FinishingOperations.EditModel
@{
    ViewData["Title"] = "Edit finishing operation";

    ViewData["BackNav"] = new BackNavModel();
}

<h1 class="h3 mb-3">Edit finishing operation</h1>
<form method="post">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <partial name="_FinishingOperationForm" model="Model.Input" />
    @if (Model.CanEdit)
    {
        <button type="submit" class="btn btn-primary">Save</button>
        @if (Model.IsActive)
        {
            <button type="submit" asp-page-handler="Deactivate" class="btn btn-outline-danger"
                    onclick="return confirm('Deactivate this finishing operation?');">Deactivate</button>
        }
    }
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
