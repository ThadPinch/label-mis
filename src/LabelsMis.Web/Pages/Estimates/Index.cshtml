@page "/estimates"
@model LabelsMis.Web.Pages.Estimates.IndexModel
@{
    ViewData["Title"] = "Estimates";
    var route = new Dictionary<string, string?>
    {
        ["Search"] = Model.Search,
        ["Status"] = Model.Status?.ToString(),
        ["CustomerId"] = Model.CustomerId?.ToString(),
        ["SalesRepId"] = Model.SalesRepId?.ToString(),
        ["FromDate"] = Model.FromDate?.ToString("yyyy-MM-dd"),
        ["ToDate"] = Model.ToDate?.ToString("yyyy-MM-dd"),
        ["Sort"] = Model.Sort
    };
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h1 class="h3 mb-0">Estimates</h1>
    @if (Model.CanEdit)
    {
        <a class="btn btn-primary" asp-page="Create">New estimate</a>
    }
</div>

<form method="get" class="row g-2 mb-3">
    <input type="hidden" asp-for="Sort" />
    <div class="col-md-3">
        <input asp-for="Search" class="form-control" placeholder="Search number, customer, product" />
    </div>
    <div class="col-md-2">
        <select asp-for="Status" class="form-select" asp-items="Html.GetEnumSelectList<LabelsMis.Domain.Enums.EstimateStatus>()">
            <option value="">All statuses</option>
        </select>
    </div>
    <div class="col-md-2">
        <select asp-for="CustomerId" class="form-select" asp-items="@((IEnumerable<SelectListItem>)ViewData["CustomerOptions"]!)">
            <option value="">All customers</option>
        </select>
    </div>
    <div class="col-md-2">
        <select asp-for="SalesRepId" class="form-select" asp-items="@((IEnumerable<SelectListItem>)ViewData["SalesRepOptions"]!)">
            <option value="">All reps</option>
        </select>
    </div>
    <div class="col-md-1">
        <input asp-for="FromDate" class="form-control" type="date" />
    </div>
    <div class="col-md-1">
        <input asp-for="ToDate" class="form-control" type="date" />
    </div>
    <div class="col-md-1">
        <button type="submit" class="btn btn-outline-secondary w-100">Filter</button>
    </div>
</form>

<table class="table table-striped">
    <thead>
        <tr>
            <th>@await Html.PartialAsync("Shared/_SortHeader", new SortHeaderModel("Number", "number", Model.Sort, route))</th>
            <th>@await Html.PartialAsync("Shared/_SortHeader", new SortHeaderModel("Customer", "customer", Model.Sort, route))</th>
            <th>Product</th>
            <th>@await Html.PartialAsync("Shared/_SortHeader", new SortHeaderModel("Status", "status", Model.Sort, route))</th>
            <th>Total (max qty)</th>
            <th>@await Html.PartialAsync("Shared/_SortHeader", new SortHeaderModel("Created", "created", Model.Sort, route))</th>
            <th>@await Html.PartialAsync("Shared/_SortHeader", new SortHeaderModel("Valid until", "valid", Model.Sort, route))</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model.Result.Items)
    {
        <tr>
            <td>@item.EstimateNumber <span class="text-muted">Rev @item.RevisionNumber</span></td>
            <td>@item.CustomerName</td>
            <td>@item.SummaryDescription</td>
            <td>@item.Status</td>
            <td>@(item.HighestQtyTotal?.ToString("C2") ?? "—")</td>
            <td>@item.CreatedAt.ToString("yyyy-MM-dd")</td>
            <td>@(item.ValidUntilDate?.ToString("yyyy-MM-dd") ?? "—")</td>
            <td class="text-end">
                <a asp-page="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-primary">Open</a>
            </td>
        </tr>
    }
    </tbody>
</table>

@await Html.PartialAsync("Shared/_Pager", new PagerModel(
    Model.Result.Page,
    Model.Result.TotalPages,
    Model.Result.TotalCount)
{
    Extra = route
})
